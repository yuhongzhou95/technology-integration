spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///s_test?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: root
    password: yuhongzhou95A
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
logging:
  pattern:
    console: '%clr(%d{${LOG_DATEFORMAT_PATTERN:yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr([%X{traceId}]) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n'
#server:
#  tomcat:
#    max-swallow-size: -1